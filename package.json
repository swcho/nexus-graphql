{
  "private": true,
  "scripts": {
    "postinstall": "lerna run prepublish && ln -sfn ../nexus-graphql-generate/dist/index.js node_modules/.bin/nexus-graphql-generate && chmod +x node_modules/.bin/nexus-graphql-generate",
    "dev": "lerna run start",
    "bootstrap": "yarn install && lerna bootstrap",
    "clean": "lerna clean -y && yarn clean:packages && yarn clean:root",
    "clean:packages": "lerna exec -- rm -rf node_modules yarn.lock package-lock.json dist",
    "clean:root": "rm -rf node_modules yarn.lock package-lock.json",
    "reset": "yarn clean && yarn bootstrap",
    "build": "lerna run build",
    "watch": "lerna run --parallel watch",
    "ex-relation-install": "cd examples/loopback-example-relations; npm install",
    "ex-relation": "yarn workspace ex-relation start"
  },
  "workspaces": [
    "packages/*"
  ],
  "prettier": {
    "semi": false,
    "trailingComma": "all",
    "singleQuote": true
  },
  "license": "MIT",
  "devDependencies": {
    "lerna": "3.13.4"
  },
  "resolutions": {
    "graphql": "14.2.1"
  }
}
