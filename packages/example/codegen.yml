overwrite: true
schema: "src/generated/graphql/petstore.graphql"
documents: null
generates:
  src/generated/types/types.ts:
    plugins:
      - "typescript"
  src/generated/types/graphql.schema.json:
    plugins:
      - "introspection"
